        Array.from({ length: n }, () => [-1, 0])
    );
    n }, () => 
    const dp: [number, number][][] = Array.from({ length: 
    const n = board.length;
    const MOD = 1e9 + 7;

    dp[n - 1][n - 1] = [0, 1];
function pathsWithMaxScore(board: string[]): number[] {
